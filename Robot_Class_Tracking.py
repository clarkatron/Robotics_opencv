import cv2
import numpy as np
import argparse
import imutils

Class Robot_Track:
	def get_location(name, loc, dest):
		robot = name
		
	def move_robot(name, ip, row, col):
		
	def send_message(name, ip, message):
		
	def connect_robot(name, ip):
		
		
